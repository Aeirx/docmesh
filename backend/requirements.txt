# --- Phase 1: API scaffold (no ML deps yet — keeps first install fast) ---
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.11.4
pydantic-settings==2.8.1
python-multipart==0.0.20
SQLAlchemy==2.0.41
aiosqlite==0.21.0
alembic==1.15.2
structlog==25.1.0
sse-starlette==2.2.1

# --- Phase 2: hybrid retrieval (CPU wheels; torch resolves CPU-only from PyPI) ---
torch==2.6.0
sentence-transformers==3.4.1
faiss-cpu==1.10.0
rank-bm25==0.2.2
pymupdf==1.25.3
python-docx==1.1.2
numpy==1.26.4  # pinned <2 deliberately: faiss-cpu/thinc wheels target the numpy 1.x
               # ABI; a 2.x numpy imports fine but crashes at runtime in C extensions

# --- Phase 3: connection graph (spaCy NER + sklearn NMF/TF-IDF; scipy arrives
#     transitively with scikit-learn). The direct wheel URL keeps the spaCy model
#     inside `pip install -r requirements.txt` — no separate `spacy download`
#     step, which matters for Windows reproducibility. ---
spacy==3.8.4
scikit-learn==1.6.1
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# --- Phase 4+ (do not install yet) ---
# anthropic==0.49.0
